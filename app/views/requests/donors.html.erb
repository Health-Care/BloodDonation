 
<% content_for :title, "المتبرعين" %> 

<h1>Request active donors</h1>

<table width="80%">
  <thead>
    <tr> 
      <th>user name</th>
      <th>user phone</th>  
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
    <tr> 
      <% if current_user && user.id == current_user.id %> 
      <td>you</td>
      <td></td> 
      <% else  %>
      <td><%= user.name %></td>
      <td><%= user.phone %></td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>


